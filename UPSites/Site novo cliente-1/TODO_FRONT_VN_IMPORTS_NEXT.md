# Próximos passos — VN_IMPORTS.html

- [ ] Inserir Meta tags SEO/OG no `<head>`
- [ ] Inserir CSS do Skeleton loader imediatamente após o bloco `/* ─── RESET ─── */`
- [ ] Criar `showSkeletonGrid(n=8)` dentro do `<script>` antes de `renderGrid(list)`
- [ ] Chamar `showSkeletonGrid();` no início de `carregarProdutosDoServidor(categoria)`
- [ ] Garantir persistência do carrinho (`vn_cart`) continua intacta


